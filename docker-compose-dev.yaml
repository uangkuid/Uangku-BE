version: "3.8"

services:
  api:
    container_name: api
    image: oratakashi/uangku-be:dev
    platform: linux/amd64
    entrypoint: php artisan octane:frankenphp
    ports:
      - "80:8000"
    depends_on:
      - mariadb
      - redis
      - minio
    networks:
      - uangku-network  # <-- Referensi network di bawah

  mariadb:
    container_name: mariadb
    image: mariadb:latest
    restart: always
    ports:
      - 3306:3306
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD}
      MARIADB_ROOT_HOST: '%'
    volumes:
      - ~/mysql:/var/lib/mysql
    networks:
      - uangku-network

  redis:
    container_name: redis
    image: redis:latest
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning --requirepass ${REDIS_PASSWORD}
    volumes:
      - ~/redis:/data
    networks:
      - uangku-network

  minio:
    container_name: minio
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    restart: always
    ports:
      - '9000:9000'
      - '9001:9001'
    networks:
      - uangku-network
    volumes:
      - /home/oratakashi/minio:/bitnami/minio/data
    environment:
      - MINIO_ROOT_USER=${MINIO_USERNAME}
      - MINIO_ROOT_PASSWORD=${MINIO_PASSWORD}
      - MINIO_DEFAULT_BUCKETS=${AWS_BUCKET}
      - MINIO_ADDRESS=:9000
      - MINIO_CONSOLE_ADDRESS=:9001
    command: minio server /bitnami/minio/data

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    restart: always
    depends_on:
      - mariadb
    environment:
      PMA_HOST: mariadb
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    ports:
      - "8080:80"
    networks:
      - uangku-network

networks:
  uangku-network:  # <-- Ini bagian penting, jangan sampai typo
    driver: bridge
